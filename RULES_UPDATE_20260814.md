# 개정 규정 반영 내역

이 문서는 제공된 Rules/Data 개정 내용을 데이터 수집·전처리·학습·추론 코드에 적용한 기준입니다.

## 입력 허용 표

| 구분 | 사용 | 구현 기준 |
|---|---:|---|
| GK-2A LE1B 16채널 | 허용 | API 경로에 `/LE1B/` 포함, 영역 `KO` |
| 공식 `station_list.csv` | 허용 | `STN_ID`, `LAT`, `LON`, `ALT` 네 열만 사용 |
| 날짜·시각·좌표 파생값 | 허용 | 연·월·일, 연중일, 주기 인코딩, 공식 좌표의 위성 투영 픽셀 |
| 과거 ASOS TA/HM | 라벨로만 허용 | 학습 target으로만 merge; 추론 Dataset에서 수집기·원문·라벨 제외 |
| 수치예보·ERA5·타 기관 기상자료 | 금지 | 수집·특징·보정·대체값 모두 금지 |
| 외부 DEM·토지피복·OSM·해안선 거리 등 | 금지 | 고정 파일이어도 사용 금지 |
| 평년값·지점별 과거 평균 | 금지 | 특징·기준값·fallback·모델 메타데이터로 저장 금지 |
| ASOS lag·최근값·평가기간 조회 | 금지 | 날짜와 무관하게 추론 입력으로 사용 금지 |

## 수집 변경

- GK-2A는 `https://apihub.kma.go.kr/api/typ05/api/GK2A/LE1B/{channel}/{area}/data`만 호출합니다.
- 영역은 `KO`, 14:00 KST는 05:00 UTC 자료로 고정합니다.
- 저장 경로는 `YYYY/MM/DD/{CHANNEL}_{timestamp}.nc`를 표준으로 사용합니다.
- ASOS와 위성 응답은 완료 표시·파일 크기·형식을 검사하고 성공한 파일만 캐시합니다.
- ASOS 2019~2025년 6·7·8월 14시 수집본은 `date`, `timestamp_kst`, `STN_ID`, `TA`, `HM`만 학습 라벨로 유지합니다. 새 공식 목록의 96개 지점 ID 집합과 순서가 기존 수집 대상과 같아 라벨 API를 재조회할 필요는 없습니다.

## 전처리 변경

- 기존의 임의 16×16 타일 배정을 주요 방법에서 제외했습니다.
- 공식 `LAT/LON`을 LE1B KO Lambert Conformal Conic 격자에 투영해 지점 픽셀을 계산합니다.
- 채널 해상도 0.5/1/2km에 따라 5km·15km 반경을 픽셀 반경으로 각각 변환합니다.
- 모델 테이블의 정적 열은 공식 `STN_ID`, 위도, 경도, 고도에서만 오며, 외부 지리정보를 merge하지 않습니다.
- 지점별 계절평균·평년값·ASOS lag·최근값 특징과 그 값을 쓰는 잔차/fallback 로직을 제거했습니다.
- 전체 위성 누락에 대비한 보조 모델은 공식 좌표·고도·날짜 특징만 받아야 합니다.

## 검증 기준

- `station_list.csv`는 96행, 열 집합 `STN_ID,LAT,LON,ALT`, 결측·중복 0건을 요구합니다.
- 16채널 모두에서 96개 지점이 KO 격자 범위 안에 있어야 합니다.
- 학습 특징 열에 ASOS 평균·lag·평년값 표식자가 있으면 학습을 중단합니다.
- 추론 Dataset에 ASOS 수집기·학습 코드·라벨·API 주소가 있으면 실패 처리합니다.
- 추론 Dataset의 GK-2A API 경로는 반드시 `/LE1B/`를 포함해야 합니다.
